from fractions import Fraction

frac1 = Fraction(input("Enter the first fraction (e.g., 1/2): "))
frac2 = Fraction(input("Enter the second fraction (e.g., 1/3): "))

result = frac1 + frac2

print("The sum of", frac1, "and", frac2, "is:", result)
